{% extends 'base.html.twig' %}

{% block body %}
    
    <h3>
        {{ link.name }}
    </h3>
    <p>
        {{ link.detail }}
    </p>
    <a href="{{ link.url }}" target="_blank">
        {{ link.url }}
    </a>
    <br>
    <a href="{{ path('edit', {id : link.id}) }}">
        Editer
    </a>
    
{% endblock %}